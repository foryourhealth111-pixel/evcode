# EvCode Retro Protocol

Use this protocol after meaningful execution closure to record:

- failures
- route drift
- verification gaps
- cleanup gaps

Retro analysis may advise future changes, but it must not silently mutate runtime authority.
