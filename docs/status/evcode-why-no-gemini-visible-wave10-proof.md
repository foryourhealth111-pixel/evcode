# EvCode Why No Gemini Visible Wave10 Proof

The pasted transcript does not show a governed EvCode specialist-routed run. It shows a direct assistant session operating in another workspace (`/home/lqf/table/table2`) using local skills like `brainstorming` and repository exploration commands. There is no visible `evcode run --json` invocation, no governed runtime JSON, no `specialist_routing` object, and no specialist artifact paths such as `specialists/gemini-task-packet.json`.

Therefore Gemini is not visible there because that interaction surface is not presenting EvCode's specialist-routing receipts. It is a generic assistant workflow surface, not the EvCode governed run artifact surface.
