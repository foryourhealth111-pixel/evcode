# Governed Execution Plan

## Requirement Source

- requirement_doc: `docs/requirements/2026-03-18-evcode-unified-provider-config.md`

## Internal Grade

- grade: `M`

## Execution Steps

1. Add a single local provider env file contract and ignore it from git while keeping a tracked example.
2. Load that file in standard and benchmark entrypoints before any status, doctor, assemble, or native actions resolve provider state.
3. Surface setup path and compatibility summary through status/doctor outputs.
4. Add one canonical setup document and link it from the root README and provider runtime docs.
5. Verify with targeted unit tests and existing adapter/runtime tests.

## Ownership Boundaries

- user local overrides live in one env-style file
- assistant-policy JSON remains the tracked default source of truth
- entrypoints only project local overrides into process env
- assistant adapters continue to resolve runtime behavior from env + governed policy

## Verification Commands

- `python3 -m unittest tests.test_app_entrypoints tests.test_assistant_adapters tests.test_runtime_bridge`
- `python3 scripts/verify/check_contracts.py`

## Rollback Rules

- If local config loading breaks native launch semantics, revert to documented env-only setup and keep the docs change isolated.
- If status metadata becomes misleading, prefer removing extra fields over reporting inaccurate compatibility claims.

## Phase Cleanup Expectations

- remove `__pycache__` generated by Python tests
- avoid touching unrelated Node processes
- preserve governed traceability docs and any hook cleanup receipts already emitted
