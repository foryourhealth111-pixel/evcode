# Governed Execution Plan

## Requirement Source

- requirement_doc: `docs/requirements/2026-03-18-evcode-provider-probe.md`

## Internal Grade

- grade: `M`

## Execution Steps

1. Add a small Python probe script that reads the current assistant policy and resolved env-based provider settings.
2. Report compact statuses per assistant and attempt live validation only when configuration allows it.
3. Expose the probe through `evcode` and `evcode-bench` as a minimal JSON-capable command.
4. Document the command in the canonical provider setup doc and verify the non-live path through tests.

## Verification Commands

- `python3 -m unittest tests.test_app_entrypoints`
- `python3 scripts/verify/check_contracts.py`
- `node apps/evcode/bin/evcode.js probe-providers --json`

## Rollback Rules

- If the probe adds too much complexity to entrypoints, keep the script and remove the CLI wiring.
- If live probe behavior is unstable, preserve dry non-live reporting instead of widening runtime behavior.

## Phase Cleanup Expectations

- remove `__pycache__` generated by the new Python script tests
- preserve existing hook cleanup receipts
- do not kill unrelated Node processes
