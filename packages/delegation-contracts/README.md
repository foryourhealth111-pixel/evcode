# Delegation Contracts

This package defines the governed task packet and result packet contracts for specialist delegation.

Rules:

- specialist work is subordinate to `vibe`
- Codex remains the final executor
- malformed packets must fail deterministically
- receipts must stay safe to persist without secrets
